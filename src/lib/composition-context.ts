/**
 * Re-export from @opendash/composition — the canonical composition system.
 * This file exists for backward compatibility with existing local imports.
 */
export {
  CompositionContextReact,
} from '@opendash/composition'

export type {
  CompositionContext,
  ComponentInstance,
} from '@opendash/composition'
